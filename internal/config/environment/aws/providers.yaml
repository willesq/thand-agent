version: "1.0"
providers:

  thand:
    name: Thand Provider
    description: Thand internal provider
    provider: thand
    enabled: true

  aws:
    name: AWS Cloud Provider
    description: Amazon Web Services cloud provider
    provider: aws
    enabled: true

  email:
    name: AWS SES Email
    description: AWS Simple Email Service for notifications
    provider: email
    enabled: true
